---
slug: blog/2025/balanced-brackets
title: Balanced Brackets
authors: [luckystar]
tags: [jstask, 2025, Stack]
date: 2025-11-03
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

Given a string **str** consisting of characters such as `(`, `)`, `{`, `}`, `[` and `]`, determine if the input string is **properly balanced**.

---

A string is considered balanced if:

* **Each opening bracket is closed by the same type of bracket** (e.g., `(` with `)`, `{` with `}`, and `[` with `]`)
* **Open brackets are closed in the correct order** (e.g., `([])` is valid, but `([)]` is not)
* **Any subset of brackets enclosed within a matched pair must also form a valid matched pair** (e.g., `{[(])}` is not balanced because the contents inside `{` and `}` are unbalanced).


<Tabs>
  <TabItem value="examples" label="Examples" defaultValue>
* isBalancedBrackets("([)]") ➞ false
* isBalancedBrackets("([]){}") ➞ true
* isBalancedBrackets("[]") ➞ true

</TabItem>

<TabItem value="solution" label="Solution" >
```jsx 
function isBalancedBrackets(str) {
  let arr = [...str]
  let  s = [];
    if(str.length%2!==0 || (/^[\)\]\}]/).test(str)){return false}
    else {
        for (let i = 0; i < arr.length; i++) {
            if(arr[i].match(/[\(\[\{]/)){
                s.push(arr[i])
            } else {
                if(arr[i] === ')' && s[s.length-1]==='(' ||
                arr[i] === ']' && s[s.length-1]==='[' || 
                arr[i] === '}' && s[s.length-1]==='{') {s.pop()} else {return false}     
            }
        }
    }
    return s.length === 0
}

```
</TabItem>

</Tabs>

